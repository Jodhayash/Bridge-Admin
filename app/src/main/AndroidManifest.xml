<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bridge_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowData.blist"></activity>
        <activity android:name=".ShowData.tlist" />
        <activity android:name=".ShowData.dlist" />
        <activity android:name=".Circulars" />
        <activity android:name=".Buses" />
        <activity android:name=".Parents" />
        <activity android:name=".Teachers" />
        <activity android:name=".Students" />
        <activity android:name=".AdminHome" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>